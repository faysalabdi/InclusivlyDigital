import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Inclusively Digital Marketing | Healthcare & NDIS Digital Marketing Specialists",
  description: "Specialized digital marketing agency for healthcare and NDIS providers. Google Ads management and website development with over 10 years of industry experience.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
} 